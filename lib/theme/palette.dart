import 'package:flutter/material.dart';

class AppPalette {
  // Core
  static const primary = Color(0xFFFFD884); // Baby Yellow
  static const primaryDark = Color(0xFFF4B667); // Warm Amber
  static const secondary = Color(0xFFFF9DB5); // Blush Pink
  static const accent = Color(0xFF8AD0FF); // Baby Blue

  // Backgrounds
  static const background = Color(0xFFFFF9F0);
  static const card = Color(0xFFFFFFFF);
  static const border = Color(0xFFE7D7C8);

  // Text
  static const textPrimary = Color(0xFF3A302A);
  static const textSecondary = Color(0xFF8A7C70);

  // Status
  static const success = Color(0xFF7BCF8E);
  static const warning = Color(0xFFFF8A7A);

  // Dark mode approximations
  static const darkBackground = Color(0xFF1E1814);
  static const darkSurface = Color(0xFF2A221D);
  static const darkBorder = Color(0xFF4A4038);
}
